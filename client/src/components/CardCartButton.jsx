import React from 'react';

const CardCartButton = (props) => {
  return (
    <div>
      <button type='button' className='card-contents-cart-button'><i class="fas fa-cart-plus cart-icon"></i>add to card</button>
    </div>
  )
}

export default CardCartButton;
